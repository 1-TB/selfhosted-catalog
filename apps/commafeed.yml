slug: commafeed
name: CommaFeed
category: feeds
description: Google Reader-style feed aggregator.
homepage: https://www.commafeed.com
source: https://github.com/Athou/commafeed

image: athou/commafeed
version: "7.2.1-h2"
arch: [amd64, arm64]

port: 8082
mem_limit: 512m
database: postgres
auth: proxy

env:
  - name: QUARKUS_DATASOURCE_JDBC_URL
    value: "jdbc:postgresql://commafeed-postgres:5432/commafeed"
  - name: QUARKUS_DATASOURCE_USERNAME
    value: "commafeed"
  - name: QUARKUS_DATASOURCE_PASSWORD
    value: "${COMMAFEED_DB_PASSWORD}"
  - name: QUARKUS_HTTP_AUTH_SESSION_ENCRYPTION_KEY
    secret: true

volumes:
  - path: data
    mount: /data

notes: |
  QUARKUS_HTTP_AUTH_SESSION_ENCRYPTION_KEY must be at least 16 characters or
  every restart forces users to log back in. No OIDC support upstream.
